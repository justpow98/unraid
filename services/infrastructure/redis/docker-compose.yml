version: '3.8'
services:
  redis:
    image: redis:8.2.3
    container_name: Redis
    restart: unless-stopped
    environment:
    - TZ=America/New_York
    - HOST_OS=Unraid
    - HOST_HOSTNAME=JP-Dell
    - HOST_CONTAINERNAME=Redis
    volumes:
    - redis_data:/data
    ports:
    - 6379:6379
    networks:
    - db_net
    healthcheck:
      test:
      - CMD-SHELL
      - redis-cli ping || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    command: redis-server --appendonly yes --auto-aof-rewrite-percentage 100 --auto-aof-rewrite-min-size
      64mb
    labels:
    - net.unraid.docker.icon=https://raw.githubusercontent.com/selfhosted/unRAID-CA-templates/master/templates/img/redis-icon.png
volumes:
  redis_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ${CACHE_PATH}/redis
networks:
  db_net:
    external: true
