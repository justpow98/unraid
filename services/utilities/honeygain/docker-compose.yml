# ============================================================================
# services/utilities/honeygain/docker-compose.yml
# ============================================================================
version: '3.8'

services:
  honeygain:
    image: honeygain/honeygain:latest
    container_name: honeygain
    restart: unless-stopped
    environment:
      - TZ=${TZ}
      - HOST_OS=Unraid
      - HOST_HOSTNAME=JP-Dell
      - HOST_CONTAINERNAME=honeygain
    labels:
      - "kuma.monitoring.group.name=Utilities"
      - "kuma.uptime_kuma.name=Honeygain"
      - "kuma.uptime_kuma.url=http://honeygain:8080/ready"
      - "kuma.uptime_kuma.method=GET"
      - "kuma.uptime_kuma.interval=60"
      - "kuma.uptime_kuma.keyword=ready"
      - "net.unraid.docker.icon=https://raw.githubusercontent.com/selfhosted/unRAID-CA-templates/master/templates/img/honeygain-icon.png"
      - "net.unraid.docker.webui=http://[IP]:[PORT:8080]"
    command: -tou-accept -email ${HONEYGAIN_EMAIL} -pass ${HONEYGAIN_PASSWORD} -device ${HONEYGAIN_DEVICE_NAME}
    networks:
      - internal_net

networks:
  internal_net:
    external: true